services:
  - type: web
    name: sanjaya-sap-copilot
    env: node
    plan: free
    buildCommand: npm --prefix client install && npm --prefix client run build && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
