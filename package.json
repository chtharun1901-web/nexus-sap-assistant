{
  "name": "sanjaya-sap-operations-copilot",
  "version": "1.0.0",
  "description": "Sanjaya (सञ्जय) — Agent-Ready SAP Operations Intelligence & Grounded Copilot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "deploy": "npm run build && node server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "nodemailer": "^10.0.1"
  }
}
